# Создаём файл и записываем в него данные, а потом закрываем
with open("sample.txt", "w") as my_file:
    # my_file.write("Привет, Загидин! Ты создал файл в формате .txt")
    my_file.write(input("Привет, напиши тут текст и оно будет отображаться в файле: "))

# Открыли наш созданный файл и посморели, что там находится, чтение файла
with open("sample.txt", "r") as my_file:
    print(my_file.read())
